name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Terraform + S3 Frontend Deploy
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: terraform

    steps:
      # ── 1. Checkout ───────────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      # ── 2. AWS Credentials ────────────────────────────────────────────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            us-west-1

      # ── 3. Terraform Setup ────────────────────────────────────────────────
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.5.7"

      # ── 4. Init ───────────────────────────────────────────────────────────
      - name: Terraform Init
        run: terraform init

      # ── 5. Build Lambda packages ──────────────────────────────────────────
      - name: Build Lambda packages
        working-directory: .
        run: |
          mkdir -p terraform/modules/lambda/builds/ingestion_pkg
          pip3 install -r lambda/ingestion/requirements.txt -t terraform/modules/lambda/builds/ingestion_pkg/ --quiet
          cp lambda/ingestion/handler.py terraform/modules/lambda/builds/ingestion_pkg/

          mkdir -p terraform/modules/lambda/builds/retrieval_pkg
          pip3 install -r lambda/retrieval/requirements.txt -t terraform/modules/lambda/builds/retrieval_pkg/ --quiet
          cp lambda/retrieval/handler.py terraform/modules/lambda/builds/retrieval_pkg/

          mkdir -p terraform/modules/lambda/builds/analyst_pkg
          pip3 install -r lambda/analyst/requirements.txt -t terraform/modules/lambda/builds/analyst_pkg/ --quiet
          cp lambda/analyst/handler.py terraform/modules/lambda/builds/analyst_pkg/

      # ── 6. Plan ───────────────────────────────────────────────────────────
      - name: Terraform Plan
        run: terraform plan -out=tfplan
        env:
          TF_VAR_stock_api_key:     ${{ secrets.TF_VAR_STOCK_API_KEY }}
          TF_VAR_anthropic_api_key: ${{ secrets.TF_VAR_ANTHROPIC_API_KEY }}

      # ── 7. Apply ──────────────────────────────────────────────────────────
      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        env:
          TF_VAR_stock_api_key:     ${{ secrets.TF_VAR_STOCK_API_KEY }}
          TF_VAR_anthropic_api_key: ${{ secrets.TF_VAR_ANTHROPIC_API_KEY }}

      # ── 8. Deploy frontend to S3 ──────────────────────────────────────────
      - name: Deploy frontend to S3
        run: |
          BUCKET=$(terraform output -raw s3_bucket_name)
          echo "Deploying frontend to s3://$BUCKET"
          aws s3 sync ../frontend s3://$BUCKET --delete
